{
  "name": "volxo-whatsapp-backend",
  "version": "1.0.0",
  "description": "Backend API for Volxo WhatsApp Connection",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx puppeteer browsers install chrome && echo '=== Estrutura do Chromium instalado ===' && ls -la /opt/render/.cache/puppeteer/chrome/ 2>/dev/null && echo '=== Listando subdiretórios ===' && find /opt/render/.cache/puppeteer/chrome -maxdepth 3 -type d 2>/dev/null && echo '=== Fim da estrutura ==='",
    "postinstall": "npx puppeteer browsers install chrome || echo 'Chromium install failed, will retry on build'"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@supabase/supabase-js": "^2.39.4",
    "@wppconnect-team/wppconnect": "^1.37.8",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "nanoid": "^4.0.1",
    "node-fetch": "^3.3.2",
    "ws": "^8.13.0"
  }
}
